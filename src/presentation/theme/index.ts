// Barrel para consumidores del theme. Preferir este path sobre
// `@/presentation/theme/tokens` directo.

export {
  PALETTE,
  DARK_PALETTE,
  ON,
  SCRIM,
  SHADOW,
  FOCUS,
  DISABLED,
  RIPPLE,
  tokens,
  FALLBACK_PALETTE,
  colorForId,
} from './tokens';

export type {
  PaletteJson,
  DarkPalette,
  OnTokens,
  ScrimTokens,
  ShadowToken,
  ShadowTokens,
  FocusTokens,
  DisabledTokens,
  RippleTokens,
  Tokens,
} from './tokens';
